---
// Reużywalne pudełko „do pobrania" (PDF + Word) dla poradników i stron tematów.
interface FileLink {
  href: string;
  label: string;
  primary?: boolean;
}
interface Props {
  title?: string;
  description?: string;
  files: FileLink[];
  ebookHint?: boolean;
}
const {
  title = "📄 Pobierz wzór – PDF i Word",
  description,
  files,
  ebookHint = false,
} = Astro.props;
---

<div class="download-box">
  <p class="download-title">{title}</p>
  {description && <p class="download-desc">{description}</p>}
  <div class="download-actions">
    {
      files.map((f) => (
        <a
          class={`download-btn${f.primary === false ? " download-btn-alt" : ""}`}
          href={f.href}
          download
        >
          {f.label}
        </a>
      ))
    }
  </div>
  {ebookHint && (
    <p class="download-hint">
      Chcesz przejść przez pisanie każdego z tych rozdziałów krok po kroku?
      Zobacz ebook <a href="/sklep/praca-magisterska-ebook/">„Jak napisać pracę magisterską od A do Z"</a> (250 stron, 39 zł).
    </p>
  )}
</div>

<style>
  .download-box {
    border: 1px solid #c7d2fe;
    border-left: 4px solid #4f46e5;
    border-radius: 0.5rem;
    background: #f5f7ff;
    padding: 1.25rem 1.5rem;
    margin: 1.5rem 0;
  }
  .download-title {
    font-weight: 700;
    color: #3730a3;
    margin-top: 0;
  }
  .download-desc {
    margin-bottom: 0.75rem;
  }
  .download-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }
  .download-btn {
    display: inline-block;
    background: #4f46e5;
    color: #fff;
    font-weight: 600;
    padding: 0.6rem 1.4rem;
    border-radius: 0.5rem;
    text-decoration: none;
  }
  .download-btn:hover {
    background: #4338ca;
  }
  .download-btn-alt {
    background: #fff;
    color: #4f46e5;
    border: 2px solid #4f46e5;
    padding: calc(0.6rem - 2px) calc(1.4rem - 2px);
  }
  .download-btn-alt:hover {
    background: #eef2ff;
  }
  .download-hint {
    margin: 0.9rem 0 0;
    font-size: 0.875rem;
    color: #4b5563;
  }
  .download-hint a {
    color: #4f46e5;
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .download-hint a:hover {
    color: #4338ca;
  }
</style>
